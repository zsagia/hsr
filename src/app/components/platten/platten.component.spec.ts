/* tslint:disable:no-unused-variable */

import { PlattenComponent } from './platten.component';

describe('Component: Platten', () => {
  it('should create an instance', () => {
    let component = new PlattenComponent();
    expect(component).toBeTruthy();
  });
});
