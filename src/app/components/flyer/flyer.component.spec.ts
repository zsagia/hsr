/* tslint:disable:no-unused-variable */

import { FlyerComponent } from './flyer.component';

describe('Component: Flyer', () => {
  it('should create an instance', () => {
    let component = new FlyerComponent();
    expect(component).toBeTruthy();
  });
});
