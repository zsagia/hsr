/* tslint:disable:no-unused-variable */

import { FotosComponent } from './fotos.component';

describe('Component: Fotos', () => {
  it('should create an instance', () => {
    let component = new FotosComponent();
    expect(component).toBeTruthy();
  });
});
